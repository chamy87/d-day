Text input; uppercase micro-label above, hint below, amber focus. Use `mono` + `size="lg"` for the league-ID hero field.

```jsx
<Input label="Sleeper league ID" mono size="lg" placeholder="e.g. 992093874321…"/>
```
