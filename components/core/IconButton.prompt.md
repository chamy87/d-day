Icon-only square button for toolbars, filters, row actions. Always pass `label`.

```jsx
<IconButton label="Add to queue" active><PlusSvg/></IconButton>
```

Props: label (required), size (sm|md|lg), active (amber toggled state), disabled.
