/** Hover tooltip for stat abbreviations (VBD, ADP) and truncated names. */
export interface TooltipProps {
  label: string;
  children?: React.ReactNode;
}
export declare function Tooltip(props: TooltipProps): JSX.Element;
